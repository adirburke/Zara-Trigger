import XCTest
@testable import Zara_Trigger

final class Zara_TriggerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Zara_Trigger().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
